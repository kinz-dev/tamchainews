{
  "name": "tamchainews",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cantonese text-to-speech reader for the sesame daily news digest",
  "scripts": {
    "test": "node --test tests/*.test.js && python3 -m unittest discover -s tests -p 'test_*.py'"
  }
}
